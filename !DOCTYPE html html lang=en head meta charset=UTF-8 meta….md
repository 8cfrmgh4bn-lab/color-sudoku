```
<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport"content="width=device-width, initial-scale=1.0"> <title>Physical Color Sudoku Generator</title> <style> body { font-family: sans-serif; text-align: center; background: #f4f4f9; padding: 20px; } .controls { margin-bottom: 20px; } button { padding: 10px 20px; font-size: 16px; cursor: pointer; background: #4CAF50; color: white; border: none; border-radius: 5px; } .sudoku-container { display: flex; justify-content: center; gap: 40px; flex-wrap: wrap; } .board-wrapper { background: white; padding: 15px; border-radius: 8px; box-shadow: 0 4px 6px rgba(0,0,0,0.1); } .grid { display: grid; grid-template-columns: repeat(9, 40px); background: #000; border: 4px solid #000; } .cell { width: 40px; height: 40px; background: #fff; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 14px; border: 1px solid #ccc; box-sizing: border-box; } /* Thicker lines for 3x3 grids */ .cell:nth-child(3n) { border-right: 3px solid #000; } .cell:nth-child(9n) { border-right: none; } .row-break { grid-column: span 9; height: 3px; background: #000; } @media print { .controls { display: none; } body { background: white; } } </style> </head> <body> <h1>Color Sudoku Generator</h1> <p>Generate grids to copy over to your physical game pieces.</p> <div class="controls"> <label>Difficulty (Holes to punch): </label> <inputtype="number" id="holes" value="45" min="20" max="64" style="width: 50px; padding: 5px;"> <button onclick="generateNewGame()">Generate Game</button> <button onclick="window.print()"style="background:#2196F3; margin-left:10px;">Print Page</button> </div> <div class="sudoku-container"> <div class="board-wrapper"> <h3>PUZZLE BOARD</h3> <div id="puzzle-grid"class="grid"></div> </div> <div class="board-wrapper"> <h3>ANSWER KEY</h3> <div id="solution-grid" class="grid"></div> </div> </div> <script> const COLOR_MAP = { 1:'R', 2:'O', 3:'Y', 4:'LG', 5:'DG', 6:'LB', 7:'DB', 8:'LP', 9:'DP' }; function generateBaseGrid() { let board = Array.from({length: 9}, () => Array(9).fill(0)); // Simple backtracking generator for a valid Sudoku function isValid(b, row, col, num) { for (let x = 0; x < 9; x++) if (b[row][x] === num || b[x][col] === num) return false; let startRow = row - row % 3, startCol = col - col % 3; for (let i = 0; i < 3; i++) for (let j = 0; j < 3; j++) if (b[i + startRow][j + startCol] === num) return false; return true; } function solve(b) { for (let row = 0; row < 9; row++) { for (let col = 0; col < 9; col++) { if (b[row][col] === 0) { let nums = [1,2,3,4,5,6,7,8,9].sort(() => Math.random() - 0.5); for (let num of nums) { if (isValid(b, row, col, num)) { b[row][col] = num; if (solve(b)) return true; b[row][col] = 0; } } return false; } } } return true; } solve(board); return board; } function drawGrid(elementId, matrix, isPuzzle = false, holes = 45) { const container = document.getElementById(elementId); container.innerHTML = ''; // To make a puzzle, pick random indices to hide let hiddenIndices = new Set(); if (isPuzzle) { while(hiddenIndices.size < holes) { hiddenIndices.add(Math.floor(Math.random() * 81)); } } let cellCounter = 0; for (let r = 0; r < 9; r++) { if (r > 0 && r % 3 === 0) { let divider = document.createElement('div'); divider.className = 'row-break'; container.appendChild(divider); } for (let c = 0; c < 9; c++) { let cell = document.createElement('div'); cell.className = 'cell'; if (isPuzzle && hiddenIndices.has(cellCounter)) { cell.innerText = '.'; cell.style.color = '#aaa'; } else { cell.innerText = COLOR_MAP[matrix[r][c]]; } container.appendChild(cell); cellCounter++; } } } function generateNewGame() { const holes = parseInt(document.getElementById('holes').value) || 45; const solvedBoard = generateBaseGrid(); drawGrid('puzzle-grid', solvedBoard, true, holes); drawGrid('solution-grid', solvedBoard, false); } // Generate first game on load generateNewGame(); </script> </body> </html>

```
